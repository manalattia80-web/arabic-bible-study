{
  "name": "arabic-bible-study-root",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
