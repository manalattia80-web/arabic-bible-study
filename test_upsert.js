require('dotenv').config({path: '.env'});
const { createClient } = require('@supabase/supabase-js');

async function test() {
    // I need the user's supabase URL and Key to test... I don't have it!
    console.log("I cannot test without keys.");
}
test();
